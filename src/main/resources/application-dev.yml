spring:
  config:
    activate:
      on-profile: dev

server:
  port: 8081
  servlet:
    context-path: /storefront

  datasource:
    url: jdbc:h2:mem:storefront;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  rabbitmq:
    host: jaragua.cloudamqp.com
    port: 5672
    username: DIOTeam
    password: HGJr1@_mQ
    virtual-host: /
    listener:
      simple:
        acknowledge-mode: auto
    template:
      default-receive-queue: product-change-availability
    queue:
      product-change-availability: product-change-availability
